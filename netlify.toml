[build]
  base = "HunerWeb/web"
  command = "npm run build"
  publish = "HunerWeb/web/out"

[build.environment]
  NODE_VERSION = "20"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
